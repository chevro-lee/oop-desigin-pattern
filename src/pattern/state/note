
状态模式的好处：
将特定状态相关的行为封装，局部化如example1包中的ConcreteStateA和ConcreteStateB，
并且将不同的状态行为分割开来，减少了状态之间的依赖，以及消除了庞大的分支条件语句（大的分支条件会使代码难以修改和扩展）

什么时候使用状态模式：
当业务逻辑根据状态而变化时，就可以考虑使用状态模式了，将每一个业务状态定义为State的子类，并处理相应的业务逻辑
通过Context请求状态的更改


